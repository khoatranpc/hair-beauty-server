export enum Collections {
  users = "users",
  categories = "categories",
  products = "products",
  orders = "orders",
  carts = "carts",
  shops = "shops",
}

export enum TypeOfCategory {
  product = "product",
  blog = "blog",
}
